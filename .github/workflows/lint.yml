name: Lint

on:
  push:
    branches: [ main ]
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - 'Makefile'
      - '.github/workflows/lint.yml'
  pull_request:
    branches: [ main ]
    paths:
      - '**.go'
      - 'go.mod'
      - 'go.sum'
      - 'Makefile'
      - '.github/workflows/lint.yml'

jobs:
  golangci:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.0'
<<<<<<< HEAD
          cache: true

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: web-app/package-lock.json

      - name: Install Buf
        uses: bufbuild/buf-setup-action@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Web UI
        run: make web-build
=======
          cache: false
>>>>>>> 38d40fd (feat: checkpoint system with fork, history detection, and socket registry (#18))

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
<<<<<<< HEAD
          version: latest
          args: --no-config --disable-all --enable=nilnil,ineffassign,govet
=======
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: web-app/package-lock.json

      - name: Install buf
        run: |
          curl -sSL "https://github.com/bufbuild/buf/releases/download/v1.41.0/buf-Linux-x86_64" \
            -o /usr/local/bin/buf && chmod +x /usr/local/bin/buf

      - name: Install web-app dependencies
        working-directory: web-app
        run: npm ci

      - name: Generate protobuf code
        run: buf generate proto

      - name: Create web dist stub
        run: |
          mkdir -p server/web/dist
          printf '<!DOCTYPE html><html><body></body></html>' > server/web/dist/index.html

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.11.4
          args: --timeout=3m --max-issues-per-linter=0 --max-same-issues=0
          skip-cache: false
          only-new-issues: true
>>>>>>> 38d40fd (feat: checkpoint system with fork, history detection, and socket registry (#18))

      - name: Check formatting
        run: |
          if [ -n "$(gofmt -l .)" ]; then
            echo "The following files are not formatted correctly:"
            gofmt -l .
            echo "Fix these issues by running 'gofmt -w .'"
            exit 1
          fi
