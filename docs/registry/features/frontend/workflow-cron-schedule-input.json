{
  "id": "workflow-cron-schedule-input",
  "type": "frontend",
  "name": "Workflow cron schedule widget (live explanation + Simple/Advanced builder)",
  "component": "CronScheduleInput",
  "path": "web-app/src/components/workflows/CronScheduleInput.tsx",
  "filePath": "web-app/src/components/workflows/CronScheduleInput.tsx",
  "markerLine": 2,
  "tested": true,
  "testIds": [
    "CronScheduleInput > starts in Simple mode for an empty value and shows the empty-state explanation",
    "CronScheduleInput > Simple mode: daily frequency + time produces the correct cron string",
    "CronScheduleInput > Simple mode: weekly frequency exposes a day-of-week select and produces the correct cron string",
    "CronScheduleInput > Simple mode: weekdays frequency produces a 1-5 range",
    "CronScheduleInput > Simple mode: monthly frequency exposes a day-of-month input",
    "CronScheduleInput > shows an inline alert for an invalid Advanced expression",
    "CronScheduleInput > starts in Advanced mode for an expression the builder can't represent, and toggling to Simple shows a fallback notice instead of switching",
    "CronScheduleInput > Simple -> Advanced toggle preserves the exact builder-computed cron string",
    "CronScheduleInput > shows a server-local timezone label",
    "workflow-cron-schedule > Advanced mode: raw cron entry still works unchanged for power users",
    "workflow-cron-schedule > blocks submission client-side when the Advanced cron expression is invalid",
    "workflow-cron-schedule > Simple mode: building a weekly schedule via dropdowns creates the expected cron string",
    "workflow-cron-schedule > has no critical or serious Axe violations in Simple or Advanced mode"
  ],
  "lastModified": "2026-07-22T00:00:00Z"
}
