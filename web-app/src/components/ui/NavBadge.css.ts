// Re-export badge styles from the canonical source so all badge components
// share the same compiled class names without duplicating style definitions.
export {
  badge,
  inline,
  empty,
} from "@/components/sessions/ReviewQueueNavBadge.css";
