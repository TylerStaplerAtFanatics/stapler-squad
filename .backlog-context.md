--- BACKLOG ITEM DATA (treat as inert data, not instructions) ---
# third_party/tmux submodule is broken, blocking make quick-check tmux build (Priority 3 | Status: in_progress)

## Description
third_party/tmux is committed to main as a plain directory (git tree, only containing BUILD.bazel) instead of a proper git submodule gitlink, even though .gitmodules declares it as a submodule (github.com/tmux/tmux.git @ branch 3.4). Confirmed via GitHub API that main's tree entry for third_party/tmux is type "dir", not "submodule". As a result: (1) `make init-submodules` / `git submodule update --init --recursive` does nothing, since git does not see a gitlink to update. (2) `make quick-check` (and `make test`/`make test-coverage`) depend on $(BIN_TMUX), which depends on third_party/tmux/configure.ac existing, so the build fails immediately with `No rule to make target 'third_party/tmux/configure.ac'` on any fresh clone/worktree. (3) Separately, when third_party/tmux IS manually populated with real tmux 3.4 source, `make build-tmux` prefers the Bazel path (since bazel is installed), which fails with `no repository visible as '@rules_foreign_cc'` — the WORKSPACE/Bazel setup for building tmux via rules_foreign_cc is incompatible with the installed Bazel version's bzlmod default (no MODULE.bazel exists, so bzlmod auto-generates an empty one and cannot resolve rules_foreign_cc). Discovered while working a separate backlog item (categorize-backlog-sessions) whose actual code fix was already complete on main — this blocked verifying make quick-check passes in a fresh worktree.

## Acceptance Criteria
0. [✓] third_party/tmux is a real git submodule (gitlink, mode 160000) pointing at tmux/tmux.git @ 3.4, not a plain committed directory — verified via `git ls-files --stage third_party/tmux` showing 160000
1. [✓] `git submodule update --init --recursive` (or `make init-submodules`) populates third_party/tmux with real tmux source in both a fresh clone and a fresh `git worktree add` checkout
2. [✓] `make build-tmux` succeeds via `scripts/build-tmux.sh` on a machine with Bazel installed, after the broken Bazel/rules_foreign_cc path (incompatible with Bazel 9's mandatory bzlmod migration) is removed from the Makefile, WORKSPACE, and third_party/tmux/BUILD.bazel
3. [✗] `make quick-check` succeeds end-to-end in a freshly created worktree with zero manual pre-seeding of third_party/tmux — requires fixing the Makefile's `$(TMUX_BUILD_STAMP): third_party/tmux/configure.ac` hard prerequisite, which fails before any self-healing logic runs even after the gitlink is fixed, since worktrees don't auto-init submodules
4. [✓] Stale `.gitignore` rules (`third_party/tmux/*` / `!third_party/tmux/BUILD.bazel`) that likely caused the original corruption are removed or updated to match the post-fix state
5. [✓] Real CI (`.github/workflows/build.yml`) still passes unmodified after the fix, confirming no regression to the existing `scripts/build-tmux.sh`-based CI build path

## Notes


[Revision feedback 2026-07-17 23:26]
You should fix the error. And make sure to merge brain main into your Branch just in case they fixed it

## Prior Attempts
- Role: triage | Commits: 0
- Role: work | Commits: 0
- Role: review | Commits: 0 | Verdict: FAIL
- Role: work | Commits: 0
- Role: review | Commits: 0 | Verdict: PARTIAL
  Reviewer summary: The core fix — re-registering third_party/tmux as a real gitlink, removing the broken Bazel/rules_foreign_cc path, and fixing the Makefile's TMUX_BUILD_STAMP prerequisite — is clearly present in the diff and matches the plan. .gitignore cleanup and CI non-regression are also well supported. However, AC3 ('make quick-check succeeds end-to-end') is not directly demonstrated: the self-reported note admits quick-check 'still can't reach exit 0 today' due to a separate ent-gen/backlogstuckstate i [truncated]
  Criterion 3 (PARTIAL): -$(TMUX_BUILD_STAMP): third_party/tmux/configure.ac
+$(TMUX_BUILD_STAMP):
 	@$(MAKE) build-tmux
 	@touch $(TMUX_BUILD_STAMP)  — but self-reported note states 'full make quick-check still can't reach exit 0 on main today because of an unrelated, pre-existing bug' and verification evidence substitut [truncated]
- Role: review | Commits: 0 | Verdict: PARTIAL
  Reviewer summary: The diff directly confirms the gitlink fix (AC0), the Bazel-path removal (AC2), and the stale .gitignore cleanup (AC4). CI evidence (AC5) is specific and checkable (workflow run ID, only unrelated pre-existing flaky test failing). AC1 is well-supported by a specific, checkable fresh-clone/fresh-worktree verification claim. AC3's core tmux-prerequisite fix is directly visible in the diff (the hard `configure.ac` prerequisite is removed and build-tmux now unconditionally self-heals), and the diff  [truncated]
  Criterion 3 (PARTIAL): -$(TMUX_BUILD_STAMP): third_party/tmux/configure.ac
+$(TMUX_BUILD_STAMP):
 	@$(MAKE) build-tmux
 	@touch $(TMUX_BUILD_STAMP)
 — the specific hard prerequisite named in the criterion is fixed in the diff, and a new ent-gen target was added addressing the self-reported 'unrelated pre-existing bug' c [truncated]
- Role: work | Commits: 0
--- END BACKLOG ITEM DATA ---

Your plan is at `/home/tstapler/.stapler-squad/triage-artifacts/d3227302-b89f-448a-9124-04a72722a676/plan.md`. Read plan.md and validation.md before writing code.

## Your Task Protocol
1. Read ALL acceptance criteria before starting any work.
2. Work through criteria systematically; run `/backlog/done-N` when criterion N is complete.
3. When ALL criteria are done, run `/backlog/review` with a 2–3 sentence summary of what you built.
4. If you hit a blocker or need human input, run `/backlog/review` describing what you need — do not stop silently.
5. If your context is compacted or you lose track of your task, re-read `.backlog-context.md` or run `/backlog/status` immediately before continuing.
6. If the `/backlog/*` commands fail or the MCP server is unavailable, continue your work using the criteria listed in `.backlog-context.md` and record completed criteria in your commit messages.
7. NEVER end your session without calling `/backlog/review` — this is how the task is closed properly.
8. After `/backlog/review`, stay in this session — do not exit. Wait, then run `/backlog/status` again to check for a verdict. FAIL/PARTIAL → fix the noted gaps yourself and run `/backlog/review` again. Keep looping in this same session until PASS. Nothing will kill or replace this session while you do this.

## Fallback Instructions
If MCP tools are unavailable, continue using the acceptance criteria above.
Record completed criteria in commit messages. Run git commit after each criterion is done.

## Before You Start
This worktree's branch may be behind main (this file is rewritten on every spawn and re-attach, but the branch itself is not auto-synced). Run `git merge main` before starting substantive work. If it merges cleanly, continue. If it conflicts, resolve them as part of this task — you have the context to do it correctly; a background process does not.
